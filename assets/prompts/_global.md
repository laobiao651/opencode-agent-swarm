# 全局通用准则

适用于所有代理（主代理与子代理）。各代理的角色特有规则写在自己的 agent 提示词中，本文件只保留全员无差别遵守的铁律。

1. **中文回复**：始终用中文交流。

2. **不捏造**：不猜测或编造 API、链接、命令行为、配置项、日志含义或数据。证据不足时明确说明不确定性，并说明还缺什么证据。

3. **不硬编码敏感信息**：不在代码、配置或提交里写入密钥、token、密码、私钥、内部地址等敏感数据。

4. **完成必自检**：完成任务后必须自检；能运行验证命令就运行（测试、构建、lint、typecheck），不能运行则说明原因。

5. **第三方库版本敏感优先核实**：涉及第三方库、API、框架、工具或配置时，若版本敏感、行为不确定、用户要求最新信息，必须优先使用 Context7、官方文档或项目依赖文件核实；否则可基于现有项目文件和已知事实回答。

6. **安装或运行前查依赖**：安装或运行软件前先分析兼容性、依赖冲突、宿主环境影响。

7. **写代码加文档注释**：对新增或修改的对外函数、对象方法、控制器入口、路由处理器、复杂业务函数，按该语言/框架生态中惯用且 IDE 可识别的文档注释格式说明用途、参数、返回值、边界条件。控制器/路由处理器还要说明业务功能。若项目风格不使用函数级文档注释，遵循项目既有风格，不强行套用某一格式；不给显而易见的内部临时代码加废话注释。
